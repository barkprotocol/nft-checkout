export interface DonationInput {
    address: string;
    donationDate: Date;
    totalParticipants: string;
    bountyAmount: string;
  }
  
  export interface ParticipantInput {
    name: string;
  }
  